[project]
name = "easyagent"
version = "0.1.0"
description = "Hand-rolled ReAct Agent in pure Python (no frameworks)"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "openai>=1.0.0",
    "python-dotenv>=1.0.0",
]

[project.scripts]
easyagent = "main:main"
